import React from 'react'

class AddRecipeContainer extends React.Component {
  state = {
    recipes = null
  }
  render(){
    const recipes = this.state.recipes && this.state.recipes.map(
      recipe => ({
        recipe.
      })
    )

  {
    return (
      <div>
        AddRecipeContainer
      </div>
    )
  }
}


export default AddRecipeContainer